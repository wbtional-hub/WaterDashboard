# AI_MEMORY.md

## 项目长期记忆

1. 本项目是“统一智慧水务大屏系统”。
2. 本系统是独立部署、独立启动、独立运行的智慧水务大屏微服务产品。
3. 系统不与任何既有水务业务系统绑定启动。
4. 系统通过配置数据源、SQL、API、GIS 服务、三维资源、组件模板和发布版本，对接任意水务系统。
5. 后续所有设计和开发都必须基于 V17 主设计文档继续增补或优化，不得另起零散方案。
6. 平台数据库固定使用 PostgreSQL。
7. 空间数据库固定使用 PostgreSQL + PostGIS。
8. 二维 GIS 固定使用 OpenLayers。
9. 三维组件固定使用 Babylon.js。
10. 普通图表组件使用 Apache ECharts。
11. 关系拓扑、调度链路、AI 证据链使用 @antv/g6。
12. 图标与轻量动效使用 SVG / Iconfont / CSS / Lottie。
13. AI 模型调用必须通过 AI Gateway，不允许前端或普通业务接口直连 AI 模型。
14. 权限控制必须覆盖大屏、卡片、数据源、SQL、GIS 图层、三维资源、组件模板、AI 上下文、日志诊断。
15. 必须设计知识产权保护、软件授权、防盗版、防二次销售、组件签名、资产水印、License Center 等机制。

## 产品与开发方向

- 平台必须是可配置、可拖拽、可保存、可预览、可发布、可回滚、可诊断的产品，而不是写死页面和数据的静态大屏。
- 外部水务业务系统按现状通过只读数据源或适配接口接入，不得将某一家水务系统的数据结构固化为平台结构。
- 编辑态、预览态、发布态、浏览态必须分离，浏览页只加载已发布版本。
- 所有异常必须可追踪，单个卡片异常不能影响整张大屏。
- AI 是辅助能力，不是大屏主体，也不能绕过权限或读取未授权上下文。
