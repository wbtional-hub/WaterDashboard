# CODEX_NEXT_TASK.md

## 下一次任务

第一阶段第 7 步：开发卡片实例基础添加与画布草稿保存。

下一次开发前必须执行 `AGENTS.md` 中规定的强制读取和强制输出流程。

## 下一步任务范围

1. 在编辑器中支持从组件模板库添加一个基础卡片实例到草稿 `cards` 数组。
2. 卡片实例只保存基础布局和模板引用，不执行 SQL，不绑定数据源。
3. 支持卡片实例在画布上的基础展示。
4. 支持选中卡片并编辑基础属性：标题、位置、尺寸、启用状态、AI 启用预留开关。
5. 保存草稿继续只更新 `dashboard_draft`，不影响 `dashboard_version`。
6. 后端如现有草稿接口足够，优先复用；如需补充校验，只做草稿结构校验。
7. 保持 `cards` 数组可序列化、可保存、可发布预留、可回滚预留。
8. 操作继续写入 `platform.audit_log`。
9. 所有接口继续返回 `success`、`code`、`message`、`data`、`traceId`。
10. 保持数据源配置中心、组件模板管理、大屏管理和编辑器空画布能力不受影响。

## 下一步禁止事项

1. 不要一次性开发全部页面。
2. 不要开发完整 AI 模型调用。
3. 不要开发复杂 GIS 和三维。
4. 不要写死演示数据。
5. 不要绕过 `AI_MEMORY.md` 和 `AI_DEVELOPMENT_RULES.md`。
6. 不要破坏 `AGENTS.md` 约束。
7. 不要将真实数据库密码、Token、连接串或密钥提交到仓库。
8. 不要在平台配置库中写死某一家水务系统的数据结构。
9. 不要通过关闭 PostGIS 校验或手工绕过 Flyway 来伪造迁移成功。
10. 不要开发卡片 SQL 预览执行功能。
11. 不要开发复杂拖拽、AI Gateway、GIS、三维、G6、完整权限中心或 License Center。
12. 不要把大屏配置写死成某一家水务系统的页面。
13. 不要修改已发布版本快照表的不可变约束。
14. 不要开发发布版本、发布体检或浏览态大屏。
15. 不要在卡片实例中写入演示业务数据或 SQL。
