# CODEX_NEXT_TASK.md

## 下一次任务

第一阶段最小闭环开发的第 1 步：建立独立启动工程骨架。

进入编码前必须先确认基础工程选型，并执行 `AGENTS.md` 中规定的强制读取和强制输出流程。

## 下一步任务范围

1. 建立后端服务工程。
2. 建立前端工程。
3. 建立 PostgreSQL/PostGIS `docker-compose` 或本地连接配置。
4. 建立统一响应结构 `success`、`code`、`message`、`data`、`traceId`。
5. 建立全局异常处理。
6. 建立日志 `traceId` 机制。
7. 建立数据库迁移目录。
8. 建立最基础的健康检查接口。
9. 建立前端基础路由和页面框架。
10. 暂不开发完整业务功能。

## 下一步禁止事项

1. 不要一次性开发全部页面。
2. 不要开发完整 AI 模型调用。
3. 不要开发复杂 GIS 和三维。
4. 不要写死演示数据。
5. 不要绕过 `AI_MEMORY.md` 和 `AI_DEVELOPMENT_RULES.md`。
6. 不要破坏 `AGENTS.md` 约束。
