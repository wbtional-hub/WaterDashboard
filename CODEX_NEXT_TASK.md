# CODEX_NEXT_TASK.md

## 下一次任务

第一阶段最小闭环开发的第 2 步：建立 PostgreSQL 平台配置库与 Flyway 迁移基线。

下一次开发前必须执行 `AGENTS.md` 中规定的强制读取和强制输出流程。

## 下一步任务范围

1. 明确平台配置库初始化边界，不绑定任何外部水务业务表。
2. 建立首个 Flyway 迁移脚本和平台 schema。
3. 验证 PostgreSQL + PostGIS Docker Compose 启动。
4. 验证 PostGIS 扩展可用。
5. 建立数据库连接健康检查预留。
6. 补充开发环境数据库配置说明。
7. 保持后端在配置正确时可启用 Flyway，并记录迁移结果。
8. 暂不创建完整业务模块表，表结构必须按 ADR-001 分阶段实现。

## 下一步禁止事项

1. 不要一次性开发全部页面。
2. 不要开发完整 AI 模型调用。
3. 不要开发复杂 GIS 和三维。
4. 不要写死演示数据。
5. 不要绕过 `AI_MEMORY.md` 和 `AI_DEVELOPMENT_RULES.md`。
6. 不要破坏 `AGENTS.md` 约束。
7. 不要将真实数据库密码、Token、连接串或密钥提交到仓库。
8. 不要在平台配置库中写死某一家水务系统的数据结构。
