# AGENTS.md

## 适用范围

本文件适用于“统一智慧水务大屏系统”仓库中的所有 CODEX 开发、修改、测试、评审和文档工作。

## 开发前强制读取

每次开发前必须先读取以下文件：

1. `AI_MEMORY.md`
2. `AI_DEVELOPMENT_RULES.md`
3. `CODEX_TASK_LOG.md`
4. `CODEX_NEXT_TASK.md`
5. `docs/` 目录下最新的 V17 设计文档或其 Markdown 摘要

## 开发前强制输出

每次开发前必须先向用户输出：

1. 当前理解
2. 本次任务范围
3. 不会修改的范围
4. 风险点
5. 预计新增或修改文件清单

未完成上述读取和输出步骤，不允许直接修改代码。

## 开发过程约束

- 所有工作必须遵守 `AI_MEMORY.md` 中的长期记忆。
- 所有工作必须遵守 `AI_DEVELOPMENT_RULES.md` 中的开发红线。
- 所有设计和开发必须基于 V17 主设计文档继续增补或优化，不得另起互相矛盾的零散方案。
- 发现需求与固定技术栈、安全边界、权限边界、AI 边界或开发边界冲突时，必须先明确指出，不得静默绕过。

## 开发后强制更新

每次开发完成后必须更新：

1. `CODEX_TASK_LOG.md`
2. `CODEX_NEXT_TASK.md`

更新内容至少应记录本次目标、实际修改、验证结果、遗留风险和下一步建议任务。
