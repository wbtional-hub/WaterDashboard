services:
  postgis:
    image: postgis/postgis:16-3.4
    container_name: water-dashboard-postgis
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-water_dashboard}
      POSTGRES_USER: ${POSTGRES_USER:-water_dashboard}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in database/.env}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - water-dashboard-postgis-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-water_dashboard} -d ${POSTGRES_DB:-water_dashboard}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  water-dashboard-postgis-data:

